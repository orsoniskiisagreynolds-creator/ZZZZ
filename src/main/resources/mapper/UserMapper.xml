<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codenote.mapper.UserMapper">
    <resultMap id="userMap" type="com.codenote.entity.User">
        <result column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="avatar" property="avatar"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, nickname, email, avatar, status, create_time, update_time) VALUES (#{username}, #{password}, #{nickname}, #{email}, #{avatar}, #{status}, #{createTime}, #{updateTime})
    </insert>
    <delete id="deleteById">DELETE FROM sys_user WHERE id=#{id}</delete>
    <update id="update">UPDATE sys_user SET username=#{username}, password=#{password}, nickname=#{nickname}, email=#{email}, avatar=#{avatar}, status=#{status}, create_time=#{createTime}, update_time=#{updateTime} WHERE id=#{id}</update>
    <select id="findById" resultMap="userMap">SELECT id, username, password, nickname, email, avatar, status, create_time, update_time FROM sys_user WHERE id=#{id}</select>
    <select id="findAll" resultMap="userMap">SELECT id, username, password, nickname, email, avatar, status, create_time, update_time FROM sys_user ORDER BY id DESC</select>
    <select id="findByUserId" resultMap="userMap">SELECT id, username, password, nickname, email, avatar, status, create_time, update_time FROM sys_user WHERE user_id=#{userId}</select>
    <select id="countAll" resultType="int">SELECT COUNT(*) FROM sys_user</select>
    <select id="findByUsername" resultMap="userMap">SELECT id, username, password, nickname, email, avatar, status, create_time, update_time FROM sys_user WHERE username=#{username}</select>
</mapper>
