<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codenote.mapper.NoteCategoryMapper">
    <resultMap id="noteCategoryMap" type="com.codenote.entity.NoteCategory">
        <result column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="user_id" property="userId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO note_category (name, parent_id, sort_order, user_id, create_time) VALUES (#{name}, #{parentId}, #{sortOrder}, #{userId}, #{createTime})
    </insert>
    <delete id="deleteById">DELETE FROM note_category WHERE id=#{id}</delete>
    <update id="update">UPDATE note_category SET name=#{name}, parent_id=#{parentId}, sort_order=#{sortOrder}, user_id=#{userId}, create_time=#{createTime} WHERE id=#{id}</update>
    <select id="findById" resultMap="noteCategoryMap">SELECT id, name, parent_id, sort_order, user_id, create_time FROM note_category WHERE id=#{id}</select>
    <select id="findAll" resultMap="noteCategoryMap">SELECT id, name, parent_id, sort_order, user_id, create_time FROM note_category ORDER BY id DESC</select>
    <select id="findByUserId" resultMap="noteCategoryMap">SELECT id, name, parent_id, sort_order, user_id, create_time FROM note_category WHERE user_id=#{userId}</select>
    <select id="countAll" resultType="int">SELECT COUNT(*) FROM note_category</select>
</mapper>
